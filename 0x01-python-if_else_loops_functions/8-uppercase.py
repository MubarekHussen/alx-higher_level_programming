#!/usr/bin/python3
def uppercase(str):
    for up_chr in str:
        up = ord(up_chr)
        if(asci >= 97 and asci < 123):
            up = i - 32
            up_chr = chr(up)
        print("{}".format(up_chr), end="")
    print("{}".format(""))
